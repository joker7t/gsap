import React from 'react';
import Loader from './Loader';

const MainLoader = () => {
    return (
        <div>
            <Loader size='cover' />
        </div>
    );
}

export default MainLoader;
